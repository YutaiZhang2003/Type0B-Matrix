# h-recursion

Fixed-weight NS/R recursions and their consumers live here: Ramond sphere
blocks, torus one- and two-point blocks, collision-aware self-dual wrappers,
spectral assemblies, crossing/modularity drivers, and tests.

Use `PYTHONPATH=Code` when running from the repository root so the historical
basename imports can resolve shared c-recursion modules.
