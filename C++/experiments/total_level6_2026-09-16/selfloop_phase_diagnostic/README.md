The first total-level-6 glasses run exposed a missing (-1)^(r*s) in self-loop CCY residues for local slots (infinity,1,0). First disagreement: (0,0,3), independently of NS/R handle assignment. These saved failures are diagnostic records, superseded by the corrected run in the parent folder. The Mercedes graph has no self-loops, so its concurrent calculation never executes the changed branch.
